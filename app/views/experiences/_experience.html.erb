<%# Experience card for the index %>
<div class="card-experience">
  <%= cl_image_tag experience.photos.first&.key %>
  <div class="card-experience-infos w-100">
    <div>
      <h2><%= experience.name %></h2>
      <div class="divider-mini"></div>
      <p><strong>What We'll do:</strong></p>
      <p><%= experience.description %></p>
    </div>
    <div class="d-flex justify-content-between align-items-center">
      <div>
        <i class="fas fa-star"></i><strong>4.98 </strong>(12)
      </div>
      <div>
        <strong>From ¥<%= number_with_delimiter(experience.price, delimiter: ",") %></strong> / hour
      </div>
    </div>
  </div>
</div>
