<%# Photo gallery component for the show page %>
<div class="photo-grid mb-4">
  <% @experience.photos.each do |photo| %>
    <%= cl_image_tag photo.key, class: 'photo-grid-item' %>
  <% end %>
</div>
